using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.SceneManagement;
public class remainmenu : MonoBehaviour
{
    // Start is called before the first frame update
    public void remenu()
    {
        SceneManager.LoadScene(0);
    }
}
